apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/headlamp

namePrefix: dev-

patches:
  - target:
      kind: HelmRepository
    patch: |-
      - op: replace
        path: /metadata/name
        value: headlamp
