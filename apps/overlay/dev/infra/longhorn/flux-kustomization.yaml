apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: longhorn-system
  namespace: flux-system
spec:
  dependsOn:
    - name: longhorn-repo
  interval: 1h
  retryInterval: 2m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: apps/base/infra/longhorn
  prune: true
  wait: true
  namePrefix: dev-flux-
  patches:
  - target:
      kind: HelmRelease
      name: longhorn-release
    patch: |-
      - op: add
        path: /spec/values
        value: 
          defaultSettings:
            defaultDataPath: /var/mnt/longhorn
            deletingConfirmationFlag: "true"
          service:
            ui:
              type: NodePort
              nodePort: 30004
          persistence:
            reclaimPolicy: Retain
