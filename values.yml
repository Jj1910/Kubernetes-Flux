ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.kube.justinschoenfeld.com`)
    entryPoints:
      - web
#providers:
#  kubernetesGateway:
#    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All
